const UrlLists = {

};
Object.freeze(UrlList);

function generateMoreImages() {
  const $target = document.getElementsByClassName("MoreImages")[0];

  let text = "";
  text += "<div>";
  text += "</div>";
  $target.innerHTML = text;
}

generateMoreImages();
